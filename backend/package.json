{
  "name": "snct-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:migrate": "node --env-file=.env scripts/migrate.mjs",
    "db:cleanup": "node --env-file=.env scripts/cleanup-retention.mjs",
    "docker:up": "docker compose --env-file .env up -d",
    "docker:down": "docker compose --env-file .env down"
  },
  "dependencies": {
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
